# Table client setup (coffee-table / kiosk display)

> Placeholder — filled in during Phase 3 (deck live-drive).

The intended setup: a Foundry world per campaign on the `storyweald` system; the table machine
auto-logs into a dedicated display user with **Monk's Common Display** + **Hide Player UI**
configured for kiosk viewing; players join with normal Foundry users (local or remote). Module
configuration, display-user permissions, and Storyweald deck-link settings will be documented here.
